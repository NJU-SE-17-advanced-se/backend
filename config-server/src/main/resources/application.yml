eureka:
  instance:
    prefer-ip-address: true

server:
  error:
    include-message: always # 启用错误信息

spring:
  profiles:
    active: dev

  cloud:
    config:
      server:
        git:
          uri: https://github.com/NJU-SE-17-advanced-se/config.git
          username: ${CONFIG_SERVER_GIT_USER:Intrejan}
          password: ${CONFIG_SERVER_GIT_PASS:Ycj19990210wans}
          default-label: master
          search-paths: '*'
